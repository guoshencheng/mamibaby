# 阿里云 AI 服务配置

# ============================================================================
# 必填配置
# ============================================================================

# 阿里云 API Key（必填）
VITE_ALICLOUD_API_KEY=your-api-key-here

# 模型名称（可选，默认：qwen-turbo）
VITE_ALICLOUD_MODEL=qwen-turbo

# ============================================================================
# CORS 跨域配置
# ============================================================================

# 是否使用代理模式（推荐）
# - true: 使用 Vite 代理，解决 CORS 问题（默认，推荐本地开发）
# - false: 直接调用阿里云 API（需要阿里云支持 CORS 或在 HTTPS 环境）
VITE_USE_PROXY=true

# 直接调用时的 API 端点（仅在 VITE_USE_PROXY=false 时使用）
# VITE_ALICLOUD_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ============================================================================
# 可选配置
# ============================================================================

# 其他可用模型：
# VITE_ALICLOUD_MODEL=qwen-plus    # 平衡模式
# VITE_ALICLOUD_MODEL=qwen-max     # 高质量模式

# ============================================================================
# 使用说明
# ============================================================================
# 
# 1. 复制此文件为 .env.local
# 2. 填入您的阿里云 API Key
# 3. 根据需要调整其他配置
# 4. 重启开发服务器（npm run dev）
#
# 关于 CORS：
# - 本地开发（http://localhost）：使用代理模式（VITE_USE_PROXY=true）
# - 生产环境（HTTPS）：可以尝试直接调用（VITE_USE_PROXY=false）
# - 如遇跨域问题，保持使用代理模式
