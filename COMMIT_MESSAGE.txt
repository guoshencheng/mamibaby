feat: Add backend server with JWT authentication

Major Changes:
- Migrate from pure frontend to client-server architecture
- Add Express backend server on port 3000
- Implement JWT authentication system with login/logout
- Move API key from frontend to backend for security
- Add streaming API endpoints for story generation
- Implement request logging and error handling
- Add authentication middleware and route guards

Frontend Updates:
- Add LoginPage component with guest account support
- Create apiClient service to replace direct AI SDK calls
- Add authService for token management
- Update App.tsx with authentication flow
- Keep streaming response user experience

Backend Structure:
- server/index.ts - Express application entry
- server/config/ai.ts - AI configuration
- server/middleware/ - Auth, logging, error handling
- server/routes/ - Auth and story generation routes

Configuration:
- Add .env file for server configuration
- Update package.json with backend dependencies
- Add tsconfig.server.json for server TypeScript
- Update vite.config.ts with proxy settings

Documentation:
- Update README.md with v2.0 architecture
- Add SETUP.md quick start guide
- Update project structure documentation

Breaking Changes:
- Frontend now runs on port 3001 (dev) instead of 5173
- Backend API runs on port 3000
- Production build combines frontend and backend
- Login required to access the application
- Environment variables moved from VITE_ prefix to standard

Default Login:
- Username: guest
- Password: iamguest123
